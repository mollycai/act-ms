// 活动状态配置
const statusConfig = {
  ongoing: { color: 'green', text: '进行中' },
  upcoming: { color: 'blue', text: '即将开始' },
  ended: { color: 'grey', text: '已结束' },
};

const unknownStatus = { color: 'orange', text: '未知状态' };

export { statusConfig, unknownStatus };
